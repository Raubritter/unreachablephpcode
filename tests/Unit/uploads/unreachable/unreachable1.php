<?php   























        /*function pot($a,$b) 
        // Vorbedingung: $b >= 0 (positive Exponenten
        // Nachbedingung: z = a^b
        {
            $x = $a;
            $y = $b;
            $z = 1;
            // $x=$a, $y=$b, $z=1 y>=0
            // INV: $z * $x^$y = $a^$b ∧ y>=0
            while($y > 0) {
                // INV ∧ $y > 0 => $z * $x^($y-1)+1 = $a^$b ∧ ($y - 1) + 1 > 0
                $y = $y - 1;
                // $z * $x^$y+1=$a^$b ∧ $y + 1 > 0 => (($z*$x)/$x)*$x^$y+1 = $a^$b ∧ $y >= 0
                $z = $z * $x;
                // $z/$x*$x^$y+1=$a^$b ∧ $y >= 0 => $z * $x^$y = $a^$b ∧ $y >= 0 => INV 
            }
            // INV ∧ ¬ B => $z*$x^$y = $a^$b ∧ $y>=0 ∧ $y<=0 => $z*$x^0 = $z = $a^b = Q 
            return $z;
        }
        
        $z = 0;*/
        ?><!DOCTYPE html><html></html>
